#include "document.h"

#include <string>

class ZDocument
{
public:
    ZDocument() = default;
    ~ZDocument() = default;
    // Add methods and members as needed for document handling
    void load(const std::string& filePath) {
        // Load document from file
    }
    void save(const std::string& filePath) {
        // Save document to file
    }
    // Other document-related methods can be added here
};